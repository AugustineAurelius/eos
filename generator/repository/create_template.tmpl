//Code generated by generator, DO NOT EDIT.
package {{.PackageName}}



{{if .WithTx}}
import (
	"context"
	"fmt"
	"database/sql"

	sq "github.com/Masterminds/squirrel"
)
{{else}}
import (
	"context"
	"fmt"

	sq "github.com/Masterminds/squirrel"
)
{{end}}

{{if .WithTx}}
// Create{{.MessageName}} inserts a new {{.MessageName}} into the database.
func (r *repository) Create{{.MessageName}}(ctx context.Context, {{.MessageName | lower}} *{{.MessageName}}) error {
	if tx, ok := ctx.Value(common.TxKey).(*sql.Tx); ok {
		return create{{.MessageName}}(ctx, tx, {{.MessageName | lower}})
    } else {
		return create(ctx, r.db, {{.MessageName | lower}})
    }
}


func create{{.MessageName}}(ctx context.Context, run runner , {{.MessageName | lower}} *{{.MessageName}}) error {
	query, args := sq.Insert(Table{{.MessageName}}).
		Columns({{.Fields | columns}}).
		Values({{.Placeholders}}).PlaceholderFormat(sq.Dollar).MustSql()

	if _, err := run.Exec(ctx, query, args...); err != nil {
		return fmt.Errorf("failed to exec create query %s with args %v error = %w", query, args, err)
	}
	return nil
}
{{else}}
// Create{{.MessageName}} inserts a new {{.MessageName}} into the database.
func (r *repository) Create{{.MessageName}}(ctx context.Context,  {{.MessageName | lower}} *{{.MessageName}}) error {
	query, args := sq.Insert(Table{{.MessageName}}).
		Columns({{.Fields | columns}}).
		Values({{.Placeholders}}).PlaceholderFormat(sq.Dollar).MustSql()

	if _, err := r.db.Exec(ctx, query, args...); err != nil {
		return fmt.Errorf("failed to exec create query %s with args %v error = %w", query, args, err)
	}
	return nil
}
{{end}}
